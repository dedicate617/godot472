diff --git a/core/object/object.h b/core/object/object.h
index 451680a..6193d36 100644
--- a/core/object/object.h
+++ b/core/object/object.h
@@ -535,6 +535,7 @@ protected:
 	void _notification_backward(int p_notification);
 	virtual void _notification_forwardv(int p_notification) {}
 	virtual void _notification_backwardv(int p_notification) {}
+	virtual void _md_notificationv(int p_notification, bool p_reversed) {}
 	virtual String _to_string();
 
 	static void _bind_methods();
@@ -724,6 +725,7 @@ public:
 		} else {
 			_notification_forward(p_notification);
 		}
+		_md_notificationv(p_notification, p_reversed);
 	}
 
 	String to_string();
diff --git a/drivers/egl/egl_manager.cpp b/drivers/egl/egl_manager.cpp
index 935fb26..6e606d6 100644
--- a/drivers/egl/egl_manager.cpp
+++ b/drivers/egl/egl_manager.cpp
@@ -225,6 +225,37 @@ Error EGLManager::_gldisplay_create_context(GLDisplay &p_gldisplay) {
 
 	Vector<EGLint> context_attribs = _get_platform_context_attribs();
 	p_gldisplay.egl_context = eglCreateContext(p_gldisplay.egl_display, p_gldisplay.egl_config, EGL_NO_CONTEXT, (context_attribs.size() > 0) ? context_attribs.ptr() : nullptr);
+
+	// Fallback: some drivers (e.g. virtualized GPUs exposing only D3D feature level 10_0
+	// through ANGLE) hand back a non-ES3 config above, which then fails ES3 context
+	// creation with EGL_BAD_MATCH. Re-pick a config that explicitly requires
+	// EGL_OPENGL_ES3_BIT and retry once. This branch is only taken when the default
+	// attempt already failed, so drivers where the default path works are unaffected.
+	if (p_gldisplay.egl_context == EGL_NO_CONTEXT) {
+		EGLint attribs_es3[] = {
+			EGL_RED_SIZE, 1,
+			EGL_BLUE_SIZE, 1,
+			EGL_GREEN_SIZE, 1,
+			EGL_DEPTH_SIZE, 24,
+			EGL_RENDERABLE_TYPE, EGL_OPENGL_ES3_BIT,
+			EGL_NONE,
+		};
+		EGLint attribs_layered_es3[] = {
+			EGL_RED_SIZE, 8,
+			EGL_GREEN_SIZE, 8,
+			EGL_BLUE_SIZE, 8,
+			EGL_ALPHA_SIZE, 8,
+			EGL_DEPTH_SIZE, 24,
+			EGL_RENDERABLE_TYPE, EGL_OPENGL_ES3_BIT,
+			EGL_NONE,
+		};
+		config_count = 0;
+		eglChooseConfig(p_gldisplay.egl_display, OS::get_singleton()->is_layered_allowed() ? attribs_layered_es3 : attribs_es3, &p_gldisplay.egl_config, 1, &config_count);
+		if (eglGetError() == EGL_SUCCESS && config_count > 0) {
+			p_gldisplay.egl_context = eglCreateContext(p_gldisplay.egl_display, p_gldisplay.egl_config, EGL_NO_CONTEXT, (context_attribs.size() > 0) ? context_attribs.ptr() : nullptr);
+		}
+	}
+
 	ERR_FAIL_COND_V_MSG(p_gldisplay.egl_context == EGL_NO_CONTEXT, ERR_CANT_CREATE, vformat("Can't create an EGL context. Error code: %d", eglGetError()));
 
 	return OK;
diff --git a/platform/linuxbsd/x11/display_server_x11.cpp b/platform/linuxbsd/x11/display_server_x11.cpp
index 65c9a3e..7b4829e 100644
--- a/platform/linuxbsd/x11/display_server_x11.cpp
+++ b/platform/linuxbsd/x11/display_server_x11.cpp
@@ -6619,7 +6619,7 @@ DisplayServerEnums::WindowID DisplayServerX11::_create_window(DisplayServerEnums
 		}
 
 		/* set the titlebar name */
-		XStoreName(x11_display, wd.x11_window, "Godot");
+		XStoreName(x11_display, wd.x11_window, "MindSCADA");
 		XSetWMProtocols(x11_display, wd.x11_window, &wm_delete, 1);
 		if (xdnd_aware != None) {
 			XChangeProperty(x11_display, wd.x11_window, xdnd_aware, XA_ATOM, 32, PropModeReplace, (unsigned char *)&xdnd_version, 1);
diff --git a/platform/windows/display_server_windows.cpp b/platform/windows/display_server_windows.cpp
index e821786..9d32474 100644
--- a/platform/windows/display_server_windows.cpp
+++ b/platform/windows/display_server_windows.cpp
@@ -2651,6 +2651,8 @@ void DisplayServerWindows::_update_window_style(DisplayServerEnums::WindowID p_w
 
 	_get_window_style(p_window == DisplayServerEnums::MAIN_WINDOW_ID, wd.initialized, wd.fullscreen, wd.multiwindow_fs, wd.borderless, wd.resizable, wd.no_min_btn, wd.no_max_btn, wd.minimized, wd.maximized, wd.maximized_fs, wd.no_focus || wd.is_popup, wd.parent_hwnd, wd.no_redirection_bitmap, style, style_ex);
 
+	if (!wd.resizable)
+		style &= ~(WS_MINIMIZEBOX | WS_MAXIMIZEBOX);
 	SetWindowLongPtr(wd.hWnd, GWL_STYLE, style);
 	SetWindowLongPtr(wd.hWnd, GWL_EXSTYLE, style_ex);
 
diff --git a/platform/windows/SCsub b/platform/windows/SCsub
index 71316b7..063938e 100644
--- a/platform/windows/SCsub
+++ b/platform/windows/SCsub
@@ -134,17 +134,17 @@ if env["windows_subsystem"] == "gui":
     res_wrap_obj = env_wrap.RES(res_wrap_target, res_wrap_file)
     env_wrap.Depends(res_wrap_obj, "#core/version_generated.gen.h")
 
+    env_wrap["LIBS"] = []
     if env.msvc:
         env_wrap.Append(LINKFLAGS=["/SUBSYSTEM:CONSOLE"])
-        env_wrap.Append(LINKFLAGS=["version.lib"])
+        env_wrap.Append(LINKFLAGS=["version.lib", "shlwapi.lib"])
     else:
         env_wrap.Append(LINKFLAGS=["-Wl,--subsystem,console"])
-        env_wrap.Append(LIBS=["version"])
+        env_wrap.Append(LIBS=["version", "shlwapi"])
 
     prog_wrap = env_wrap.add_program("#bin/godot", common_win_wrap + res_wrap_obj, PROGSUFFIX=env["PROGSUFFIX_WRAP"])
     arrange_program_clean(prog_wrap)
     env_wrap.Depends(prog_wrap, prog)
-    sources += common_win_wrap + res_wrap_obj
 
 if env["d3d12"]:
     dxc_target_aliases = {
