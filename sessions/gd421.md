# Session 总结：gd421（Godot 4.2.1 → 4.7.2 迁移与 CI/CD 自动化）

> **归档时间**：2026-09-12  
> **Session 别名**：`gd421`  
> **仓库地址**：[https://github.com/dedicate617/godot472.git](https://github.com/dedicate617/godot472.git)  
> **本地工作区**：`Z:\D\work\OpenSource\mindscada-engine-ext`  
> **只读对照源**：`Z:\D\work\OpenSource\godot.4.2.1`（严格只读，原始工程）  
> **工作目标**：将原侵入式修改的 Godot 4.2.1 定制内核升级解耦至 Godot 4.7.2，建立独立外挂编排仓库 `mindscada-engine-ext`，实现跨平台编译（Windows x64 / Linux ARM32 / Linux ARM64）、自动化打包、CI/CD 构建与 GitHub Release 自动发布。

---

## 一、已完成工作（Accomplished Work）

### 1. 架构解耦与外挂编排库搭建
- **创建外挂独立仓库**：建立 `mindscada-engine-ext`，完全解耦核心引擎与自研功能，不污染上游 Godot 源码树。
- **抽取 5 大自研核心模块**（从 `godot.4.2.1` 完整剥离并重构构建逻辑）：
  1. `varmanager`（变量管理器，OPC UA / open62541）
  2. `mqttmanager`（MQTT 通信，paho-mqtt-c）
  3. `kvmanager`（键值存储，MMKV）
  4. `qtwindow`（Qt 窗口嵌入驱动）
  5. `webview`（跨平台 Web 视图驱动：Windows WebView2 / Linux WebKit2GTK / 哑实现回退）
- **动态依赖解析与模块探测**：各模块重构 `SCsub`，按 `deps/{platform}_{arch}` → `MINDSCADA_DEPS_ROOT` → 本地环境 fallback 优先级自动探测依赖头文件与静态/动态库；未找到依赖时优雅告警并禁用，避免破坏整体编译。

### 2. 补丁集原子化与 OEM 定制资产迁移
- **提取 4.2.1 定制并迁移至 4.7.2 规范**（存放在 `patches/4.7.2/`）：
  - `0001-branding-splash-icons.patch`：OEM 品牌标识、Logo、启动 Splash 适配。
  - `0002-windows-version-metadata.patch`：Windows PE 可执行文件版本元数据、版权信息注入。
  - `0003-scene-editor-ext.patch`：场景与编辑器扩展逻辑适配。
  - `0004-crash-handler.patch`：崩溃处理与日志收集机制。
  - `0005-zstd-disable-asm.patch`：解决 x86_64 宿主机交叉编译 ARM 平台时 zstd 引用 x86 汇编循环符号未定义的链接错误（针对非 x86_64 追加 `ZSTD_DISABLE_ASM`）。
- **OEM 资源存放**：在 `patches/assets/` 中保存完整的品牌图标、矢量图及 Splash 图像。

### 3. 三大核心自动化脚本
- **`scripts/sync_upstream.py`**：
  - 自动克隆/检出指定版本的官方纯净 Godot 源码（默认 `4.7.2-stable`）。
  - 按序列无冲突应用 `patches/4.7.2/*.patch`。
  - 自动部署 OEM 视觉资产至引擎目录。
- **`scripts/build.py`**：
  - 统一跨平台构建驱动，替代原先分散且易出错的 `.bat` / `.sh`。
  - 自动配置编译器环境：Windows MSVC (`vswhere`/`vcvars64`)；Linux ARM32 (`arm-linux-gnueabihf-gcc/g++`)、ARM64 (`aarch64-linux-gnu-gcc/g++`)。
  - 自动挂载 `custom_modules`，注入版本宏，配置 SCons 构建缓存（默认 20 GiB 限制），支持 `--dry-run` 模拟。
- **`scripts/package.py`**：
  - 标准化产物打包工具，收集 `bin/` 生成物：
    - Windows x64 Editor：`MindStudio_v4.7.2_Windows_x86_64.zip`（含 `WebView2Loader.dll`、Console 包装器与说明文档）。
    - Windows x64 模板：`MindSCADA_v4.7.2_export_templates_windows.tpz`（含 `version.txt`，符合 Godot 模板管理器规范）。
    - Linux ARM32 模板：`MindSCADA_v4.7.2_export_templates_pi32.tpz`（树莓派 32 位）。
    - Linux ARM64 模板：`MindSCADA_v4.7.2_export_templates_pi64.tpz`（树莓派 64 位）。
    - SHA256 校验和清单：`dist/checksums.sha256`。

### 4. GitHub Actions CI/CD 流水线与矩阵编译调试
- **配置文件**：`.github/workflows/build-matrix.yml`。
- **跨平台矩阵**：
  - `windows-x64` (`windows-latest`)：构建 editor、template_debug、template_release。
  - `linux-arm32` (`ubuntu-latest`)：通过 `gcc-arm-linux-gnueabihf` 交叉编译 template_debug、template_release。
  - `linux-arm64` (`ubuntu-latest`)：通过 `gcc-aarch64-linux-gnu` 交叉编译 template_debug、template_release。
- **重大关键 Bug 修复记录**：
  1. **Zstandard 汇编跨架构符号缺失**：Host Linux x86_64 编译 ARM 目标时，SCsub 判定不编译 `huf_decompress_amd64.S`，但 `huf_decompress.c` 宏判定启用了汇编，导致 `HUF_decompress4X1_...` 符号未定义。制作并合入 `0005-zstd-disable-asm.patch` 彻底解决。
  2. **SCons 命令行宏注入大小写兼容**：Godot 4.7.2 SConstruct 忽略大写 `CCFLAGS`/`CXXFLAGS`，更新为 lowercase `ccflags`/`cxxflags`/`cppdefines`。
  3. **ARM 交叉编译器缺失导致的非法 CPU 指令错误**：ARM32 构建出现 `g++: error: unrecognized command-line option '-mfpu=neon-fp-armv8'`，原因是 SCons 默认使用了原生 x86_64 g++。在 `build.py` 中显式指定 `CC=arm-linux-gnueabihf-gcc` / `CXX=arm-linux-gnueabihf-g++`（ARM64 同理对应 `aarch64-linux-gnu-*`），彻底解决编译失败。
  4. **Private 仓库 Actions 免费额度耗尽拦截**：识别到由于 Private 仓库配额耗尽导致任务秒挂（`The job was not started because recent account payments have failed...`），协同用户将仓库改为 Public 后配额限制解除。

### 5. GitHub Release 自动化发布全链路调通
- 支持 Git Tag 推送（`v*`）或手动 workflow_dispatch 触发。
- 每个构建 Job 构建及打包完成后，多节点并发将产物上传到统一创建的 GitHub Release 中（幂等并发安全）。
- **首个正式 Release 已成功发布并上线**：
  - **Release Tag**：`v4.7.2-20260910`
  - **Release Title**：`MindSCADA Engine v4.7.2`
  - **已上传分发资产清单**：
    1. `MindStudio_v4.7.2_Windows_x86_64.zip` (77.44 MB)
    2. `MindSCADA_v4.7.2_export_templates_windows.tpz` (52.91 MB)
    3. `MindSCADA_v4.7.2_export_templates_pi64.tpz` (122.57 MB)
    4. `MindSCADA_v4.7.2_export_templates_pi32.tpz` (118.63 MB)
    5. `checksums.sha256` (0.01 MB)

---

### 6. 全平台第三方依赖库归集完成（方案 A）
- **Windows x64**（`deps/windows_x64/`）：从本地环境提取全套 `.lib` 和 `include/`（`open62541`、`Open62541CppWrapper`、`mbedtls`、`mmkv`、`paho-mqtt-c/cpp`、`spdlog`、`openssl`、`pthreads`），已提交并推送。
- **Linux ARM32**（`deps/linux_arm32/`）：连接 Pi32（`192.167.8.234`），提取全套 ARM32 架构静态库（`.a`）及头文件，已提交并推送。
- **Linux ARM64**（`deps/linux_arm64/`）：连接 Pi64（`192.167.8.190`，Cortex-A76），在本地环境编译生成全套 AArch64 架构静态库（`.a`，含 `open62541` v1.3.9、`Open62541CppWrapper` v1.3.9、`mbedtls` v2.28.4、`MMKV` 1.3.3、`paho.mqtt.cpp`、以及 `spdlog` v1.8.5 `noexception` PIC Release/Debug），完成头文件和静态库组织，已提交并推送（commit `cd2f485`）。
- **模块链接探测通过**：三平台执行 `python scripts/build.py --dry-run` 均成功识别 `deps/{platform}_{arch}`，核心自研模块（`varmanager`, `mqttmanager`, `kvmanager`）全面启用（`module_*_enabled=yes`）。

---

### 7. Linux ARM32 交叉链接重定位修复与全平台 CI 跑通
- **问题诊断**：此前 ARM32 CI 链接阶段因 `mbedtls`（`libmbedcrypto.a`, `libmbedx509.a`）未开启 `-fPIC` 导致 GCC 链接器报 `R_ARM_THM_MOVW_ABS_NC: dangerous relocation: unsupported relocation` 失败。
- **隔离重编译**：在 Pi32 实体机（`192.167.8.234`）创建专用独立构建目录 `/mnt/sdx/dev/mbedtls/build-fpic/`，在完全不触碰、不影响既有应用引用的旧构建资产的前提下，以 `-DCMAKE_POSITION_INDEPENDENT_CODE=ON` 重新编译生成 Debug/Release 静态库，并通过 `readelf -r` 严格验证非 PIC 重定位符号已清零。
- **全平台矩阵全绿通过**：更新同步至 `deps/linux_arm32/lib/` 后推送 master，GitHub Actions Run ID `34937285830` 验证 `windows-x64`、`linux-arm64`、`linux-arm32` 三大平台编译及打包全部成功（All Green）。
- **发布新版本 Release**：已打 Tag 并推送 `v4.7.2-20260915`，正在自动执行 Release 发布与三平台全量分发包（Editor + 3 平台 Export Templates + Checksum 清单）上传。

---

## 二、未完成工作与后续建议（Pending & Future Work）

### 1. Release 产物发布状态确认
- **现状**：`v4.7.2-20260915` 流水线构建中，待发布完毕后确认 GitHub Release 页面中 5 大分发包及 SHA256 清单是否完备齐全。

### 2. Linux Web 视图（WebKit2GTK / WPE WebKit）完善
- **现状**：
  - Linux CI 环境中因未安装 webkit2gtk-4.0 开发包，`webview` 模块目前自动降级使用 `webview_dummy.cpp`（哑实现）。
- **待办**：
  - 若嵌入式边缘终端（树莓派/工业网关）需要实际渲染 Web 界面，需评估集成 WPE WebKit 或安装对应 GTK-WebKit 运行时库。

### 3. 本地真机测试与验证
- **待办**：
  - 从 GitHub Release 下载产物，在实体 Windows x64 主机上运行 `MindStudio.exe` 验证工程打开、WebView 渲染及 UI 功能。
  - 在树莓派 32 位（Raspberry Pi OS 32-bit）与 64 位硬件上导入 `.tpz` 模板，运行导出项目，验证运行稳定性与图形渲染后端（GLES3 / Vulkan）。

---

## 三、常用操作备忘清单（Cheatsheet）

```bash
# 1. 本地拉取纯净 Godot 4.7.2 源码并打入补丁
python scripts/sync_upstream.py --version 4.7.2-stable

# 2. 本地全量构建 Windows x64（Editor + Debug/Release 模板）
python scripts/build.py --platform windows --arch x86_64 --target all

# 3. 本地打包 dist/ 发布物
python scripts/package.py --platform windows --version 4.7.2

# 4. 触发远程完整 CI 构建并发布新版本 Release
git tag v4.7.2-YYYYMMDD -m "MindSCADA Engine v4.7.2 build"
git push origin v4.7.2-YYYYMMDD
```
